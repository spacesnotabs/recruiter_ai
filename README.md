### Recruiter AI

Recruiter AI is a small Python proof of concept for using an LLM-driven agent to collect job search criteria and query job data.

To run it locally, install the requirements in a Python 3.12 virtual environment, add `OPENROUTER_API_KEY` to `.env`, then run:

```powershell
.\.venv\Scripts\python.exe .\main.py
```
